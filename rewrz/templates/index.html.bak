{% extends "base.html" %}

{% block title %}{{ site_title }} - {{ tagline }}{% endblock %}

{% block head %}
{{ super() }}
{% if seo_data %}
    <!-- Dynamic SEO Meta Tags -->
    {% include "components/seo_meta.html" %}
{% endif %}
<link rel="stylesheet" href="https://unpkg.com/swiper/swiper-bundle.min.css">
<style>
    /* Custom styles for timeline arrows */
    .left-timeline .arrow::before {
        content: " ";
        height: 0;
        position: absolute;
        top: 22px;
        width: 0;
        z-index: 1;
        right: -10px;
        border: medium solid white;
        border-width: 10px 0 10px 10px;
        border-color: transparent transparent transparent white;
    }
    .right-timeline .arrow::before {
        content: " ";
        height: 0;
        position: absolute;
        top: 22px;
        width: 0;
        z-index: 1;
        left: -10px;
        border: medium solid white;
        border-width: 10px 10px 10px 0;
        border-color: transparent white transparent transparent;
    }

    /* Animation for timeline items */
    .timeline-item {
        opacity: 0;
        transition: opacity 0.5s ease-in-out, transform 0.5s ease-in-out;
    }
    .left-timeline.timeline-item {
        transform: translateX(-100px);
    }
    .right-timeline.timeline-item {
        transform: translateX(100px);
    }
    .timeline-item.is-visible {
        opacity: 1;
        transform: translateX(0);
    }
    
    /* 确保主要内容区域有正确的类名 */
    .main-content {
        transition: padding-top 0.3s ease;
    }
    
    /* 背景图片样式 */
    body.background-gradient1 {
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%) !important;
    }
    
    body.background-gradient2 {
        background: linear-gradient(135deg, #ff9a9e 0%, #fecfef 100%) !important;
    }
    
    body.background-gradient3 {
        background: linear-gradient(135deg, #a8edea 0%, #fed6e3 100%) !important;
    }
    
    body.background-gradient4 {
        background: linear-gradient(135deg, #ffecd2 0%, #fcb69f 100%) !important;
    }
    
    body.background-custom {
        background-size: cover !important;
        background-position: center !important;
        background-attachment: fixed !important;
    }
    
    /* 确保主要内容区域有适当的背景以保证可读性 */
    .main-content {
        background-color: rgba(255, 255, 255, 0.9);
        backdrop-filter: blur(10px);
    }
    
    .dark .main-content {
        background-color: rgba(15, 23, 42, 0.9);
    }
</style>
{% endblock %}

{% block content %}
{# 确保 settings 变量存在并提供默认值 #}
{% set settings = settings or {} %}
{% set homepage_mode = settings.get('homepage_mode') %}

{# 应用背景图片设置 #}
{% if settings.get('background_image_settings') %}
    {% set bg_settings = settings.get('background_image_settings') %}
    {% if bg_settings.type == 'gradient1' %}
        <script>
            document.body.classList.add('background-gradient1');
        </script>
    {% elif bg_settings.type == 'gradient2' %}
        <script>
            document.body.classList.add('background-gradient2');
        </script>
    {% elif bg_settings.type == 'gradient3' %}
        <script>
            document.body.classList.add('background-gradient3');
        </script>
    {% elif bg_settings.type == 'gradient4' %}
        <script>
            document.body.classList.add('background-gradient4');
        </script>
    {% elif bg_settings.type == 'custom' and bg_settings.custom_url %}
        <script>
            document.body.classList.add('background-custom');
            document.body.style.backgroundImage = 'url("' + '{{ bg_settings.custom_url }}' + '")';
        </script>
    {% endif %}
{% endif %}

{# 1. Hero Section (Fullscreen Gallery or Video) #}
{% if homepage_mode == 'fullscreen_gallery' %}
<div class="relative h-screen w-full overflow-hidden">
    <!-- Background Swiper -->
    <div class="absolute inset-0 z-0">
        <div id="fullscreen-gallery-swiper" class="swiper h-full w-full">
            <div class="swiper-wrapper">
                {% if settings.homepage_background_image_url %}
                    {% set images = settings.homepage_background_image_url.split('\n') %}
                    {% for image_url in images %}
                        {% if image_url.strip() %}
                        <div class="swiper-slide">
                            <img src="{{ image_url.strip() }}" alt="Background Image" class="w-full h-full object-cover">
                        </div>
                        {% endif %}
                    {% endfor %}
                {% endif %}
            </div>
            <div class="swiper-pagination"></div>
            <div class="swiper-button-prev"></div>
            <div class="swiper-button-next"></div>
        </div>
    </div>
    <!-- Overlay Content -->
    <div class="relative z-10 flex flex-col items-center justify-center h-full text-white bg-black bg-opacity-40">
        <div class="text-center p-8 bg-black bg-opacity-50 rounded-lg backdrop-blur-sm">
            <h1 class="text-5xl md:text-6xl font-bold mb-4">{{ site_title }}</h1>
            {% if tagline %}<p class="text-xl md:text-2xl mb-8">{{ tagline }}</p>{% endif %}
            <div class="flex flex-wrap justify-center gap-4 mt-8">
                <a href="/archives" class="px-6 py-3 bg-white bg-opacity-20 hover:bg-opacity-30 rounded-full transition duration-300">归档</a>
                
                <!-- 动态格式导航 -->
                {% if post_formats %}
                {% for format in post_formats %}
                <a href="/formats/{{ format.slug }}" class="px-6 py-3 bg-white bg-opacity-20 hover:bg-opacity-30 rounded-full transition duration-300">{{ format.name }}</a>
                {% endfor %}
                {% endif %}
                
                <!-- 自定义页面下拉菜单 -->
                {% if custom_pages and custom_pages|length > 0 %}
                <div class="relative group">
                    <button class="px-6 py-3 bg-white bg-opacity-20 hover:bg-opacity-30 rounded-full transition duration-300 flex items-center">
                        更多
                        <i class="fas fa-chevron-down ml-2 text-sm"></i>
                    </button>
                    <div class="absolute top-full left-0 mt-2 w-48 bg-white dark:bg-gray-800 rounded-lg shadow-lg opacity-0 invisible group-hover:opacity-100 group-hover:visible transition-all duration-300 z-50">
                        {% for page in custom_pages %}
                        <a href="/{{ page.slug }}" class="block px-4 py-2 text-gray-800 dark:text-white hover:bg-gray-100 dark:hover:bg-gray-700 transition-colors">{{ page.title }}</a>
                        {% endfor %}
                    </div>
                </div>
                {% endif %}
            </div>
        </div>
    </div>
</div>
{% elif homepage_mode == 'fullscreen_video' %}
<div class="relative h-screen w-full overflow-hidden">
    <!-- Background Video -->
    <div class="absolute inset-0 z-0">
        {% if settings.homepage_background_video_url %}
        <video autoplay muted loop playsinline class="w-full h-full object-cover">
            <source src="{{ settings.homepage_background_video_url }}" type="video/mp4">
            您的浏览器不支持视频播放。
        </video>
        {% endif %}
    </div>
    <!-- Overlay Content -->
    <div class="relative z-10 flex flex-col items-center justify-center h-full text-white bg-black bg-opacity-40">
        <div class="text-center p-8 bg-black bg-opacity-50 rounded-lg backdrop-blur-sm">
            <h1 class="text-5xl md:text-6xl font-bold mb-4">{{ site_title }}</h1>
            {% if tagline %}<p class="text-xl md:text-2xl mb-8">{{ tagline }}</p>{% endif %}
            <div class="flex flex-wrap justify-center gap-4 mt-8">
                <a href="/archives" class="px-6 py-3 bg-white bg-opacity-20 hover:bg-opacity-30 rounded-full transition duration-300">归档</a>
                
                <!-- 动态格式导航 -->
                {% if post_formats %}
                {% for format in post_formats %}
                <a href="/formats/{{ format.slug }}" class="px-6 py-3 bg-white bg-opacity-20 hover:bg-opacity-30 rounded-full transition duration-300">{{ format.name }}</a>
                {% endfor %}
                {% endif %}
                
                <!-- 自定义页面下拉菜单 -->
                {% if custom_pages and custom_pages|length > 0 %}
                <div class="relative group">
                    <button class="px-6 py-3 bg-white bg-opacity-20 hover:bg-opacity-30 rounded-full transition duration-300 flex items-center">
                        更多
                        <i class="fas fa-chevron-down ml-2 text-sm"></i>
                    </button>
                    <div class="absolute top-full left-0 mt-2 w-48 bg-white dark:bg-gray-800 rounded-lg shadow-lg opacity-0 invisible group-hover:opacity-100 group-hover:visible transition-all duration-300 z-50">
                        {% for page in custom_pages %}
                        <a href="/{{ page.slug }}" class="block px-4 py-2 text-gray-800 dark:text-white hover:bg-gray-100 dark:hover:bg-gray-700 transition-colors">{{ page.title }}</a>
                        {% endfor %}
                    </div>
                </div>
                {% endif %}
            </div>
        </div>
    </div>
</div>
{% endif %}

{# 2. Timeline Section (Always rendered) #}
<div class="bg-gray-50 dark:bg-gray-800 py-8 main-content">
    <div class="container mx-auto w-full h-full">
        <div class="relative wrap overflow-hidden p-4 md:p-10 h-full">
            <div class="absolute h-full border-2 border-gray-200 dark:border-gray-700" style="left: 50%"></div>
            
            {% for post in posts %}
                {% set format_slugs = post.formats | map(attribute='slug') | list %}

                {# Define dot and icon classes based on format #}
                {% set dot_class = "bg-gray-500" %}
                {% set icon_class = "fas fa-pen" %}
                {% if 'micro-post' in format_slugs %}
                    {% set dot_class = "bg-blue-500" %}
                    {% set icon_class = "fas fa-comment-dots" %}
                {% elif 'photo-album' in format_slugs %}
                    {% set dot_class = "bg-purple-500" %}
                    {% set icon_class = "fas fa-images" %}
                {% elif 'weibo' in format_slugs %}
                    {% set dot_class = "bg-red-500" %}
                    {% set icon_class = "fab fa-weibo" %}
                {% elif 'video' in format_slugs %}
                    {% set dot_class = "bg-green-500" %}
                    {% set icon_class = "fas fa-video" %}
                {% elif 'poetry-song' in format_slugs %}
                    {% set dot_class = "bg-yellow-500" %}
                    {% set icon_class = "fas fa-music" %}
                {% endif %}

                <!-- Timeline item -->
                <div class="mb-8 flex {{ 'justify-start' if loop.index0 % 2 == 0 else 'justify-end' }}">
                    <div class="timeline-item {{ 'left-timeline' if loop.index0 % 2 == 0 else 'right-timeline' }} flex items-center w-2/5 {{ 'flex-row-reverse' if loop.index0 % 2 != 0 }}">
                        <div class="order-1 w-5/12"></div>
                        <div class="z-20 flex items-center order-1 bg-gray-800 shadow-xl w-8 h-8 rounded-full">
                            <h1 class="mx-auto text-white text-sm font-semibold">{{ loop.index }}</h1>
                        </div>
                        <div class="order-1 bg-white dark:bg-gray-800 rounded-lg shadow-xl w-5/12 px-6 py-4">
                            <div class="flex items-center mb-2">
                                <span class="mr-2 text-xs font-semibold">{{ post.published_at.strftime('%Y-%m-%d') if post.published_at else post.created_at.strftime('%Y-%m-%d') }}</span>
                                <span class="text-xs font-semibold px-2 py-1 rounded-full {{ dot_class.replace('bg-', 'bg-') }} text-white">
                                    <i class="{{ icon_class }} mr-1"></i>
                                    {{ post.formats[0].name if post.formats else '文章' }}
                                </span>
                            </div>
                            <h3 class="font-bold text-gray-800 dark:text-white text-xl mb-2">
                                <a href="{{ post.url }}" class="hover:text-blue-600 dark:hover:text-blue-400 transition-colors">
                                    {{ post.title }}
                                </a>
                            </h3>
                            <p class="text-gray-600 dark:text-gray-300 text-sm">
                                {{ post.excerpt or post.content[:100] + '...' if post.content else '' }}
                            </p>
                            <div class="mt-3">
                                <a href="{{ post.url }}" class="text-blue-600 dark:text-blue-400 hover:underline text-sm font-semibold">
                                    阅读更多
                                </a>
                            </div>
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>
</div>

<script src="https://unpkg.com/swiper/swiper-bundle.min.js"></script>
<script>
    // Initialize Swiper if needed
    document.addEventListener('DOMContentLoaded', function() {
        if (document.getElementById('fullscreen-gallery-swiper')) {
            new Swiper('#fullscreen-gallery-swiper', {
                loop: true,
                pagination: {
                    el: '.swiper-pagination',
                    clickable: true
                },
                navigation: {
                    nextEl: '.swiper-button-next',
                    prevEl: '.swiper-button-prev'
                },
                autoplay: {
                    delay: 5000,
                    disableOnInteraction: false
                }
            });
        }

        // Timeline animation
        function animateTimelineItems() {
            const timelineItems = document.querySelectorAll('.timeline-item');
            timelineItems.forEach(item => {
                const itemPosition = item.getBoundingClientRect().top;
                const screenPosition = window.innerHeight / 1.3;
                if (itemPosition < screenPosition) {
                    item.classList.add('is-visible');
                }
            });
        }

        // Run on scroll and initially
        window.addEventListener('scroll', animateTimelineItems);
        animateTimelineItems();
    });
</script>
{% endblock %}