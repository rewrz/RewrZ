# 真实环境巡检清单（当前有效状态，2026-05-31）

本文档仅保留真实环境巡检的当前有效结论。

## 1. 当前环境

- 当前真实巡检实例：`http://127.0.0.1:8001`
- 约束：
  - 单实例 `reload` 巡检
  - 不重复开多实例制造噪音
  - 保持当前后台登录态连续使用
  - 当前 Codex 应用内浏览器已可直接访问 `http://127.0.0.1:8001`，本轮新增点验可直接在应用内浏览器完成

## 2. 当前目标

- 持续用真实浏览器巡检前台与后台页面
- 覆盖：
  - 可用性
  - 用户体验
  - 主题一致性
  - 安全边界
  - 性能感知
  - 二次元风格统一精修

## 3. 当前已确认有效的全局结论

### 3.1 前台主题系统

- 前后台主题状态源已收口到同一份全站主题源
- 主题切换后，前台首页、内页、归档页、页面模板、搜索页、媒体归档页的主视觉与核心语义色可联动更新
- `galaxy` 等深色主题已进入统一深色语义态，不再停留在“主题名切了但页面仍按浅色语义渲染”的半失效状态
- 首页封面资料卡、时间轴主轴/节点、返回顶部按钮、导航下拉溢出、媒体菜单图标、相关文章体量等前台主干问题已完成收口
- 站内静态资源 URL 已补齐本地绝对地址归一化：
  - 形如 `http://127.0.0.1:8000/static/...` 的误存值会在渲染时收口为 `/static/...`
  - 切换域名、端口或反代入口时，不再因为本地绝对地址写死而丢图

### 3.2 密码保护与评论可见链路

- 密码保护 QA 样本链路已验证通过：
  - 访客密码解锁
  - 正文展示
  - 评论提交
  - 评论后可见内容揭示
- 当前样本：
  - 页面：`/article/qa-password-comments-20260530`
  - 测试密码：`1`

### 3.3 后台主题系统

- 后台动态主题切换已覆盖主框架与高频管理页，不再只有外壳变色、内容区残留旧蓝灰
- 以下后台页面当前已完成批量视觉收口并进入统一后台主题语义层：
  - `/rewrz-admin/system-info`
  - `/rewrz-admin/error-settings`
  - `/rewrz-admin/security-center`
  - `/rewrz-admin/settings`
  - `/rewrz-admin/themes`
  - `/rewrz-admin/comments`
  - `/rewrz-admin/comment-settings`
  - `/rewrz-admin/data-management`
  - `/rewrz-admin/api-keys`
  - `/rewrz-admin/posts`
  - `/rewrz-admin/pages`
  - `/rewrz-admin/media`
  - `/rewrz-admin/categories`
  - `/rewrz-admin/tags`
  - `/rewrz-admin/users`
  - `/rewrz-admin/posts/new`

### 3.4 前台微博快捷发布

- 登录态下的前台微博快捷发布器已恢复基础可用性
- 本轮已继续确认剩余高频按钮链路可用：
  - `多媒体` 可正常打开上传界面
  - `预览` 为卡片内联预览态，可直接展示当前输入内容，不是无响应假失效
  - `话题` 可向输入框插入 `#话题#`
  - `提及` 可向输入框插入 `@`
  - `清草稿` 可清空当前输入内容，并将发布按钮恢复为禁用态
- 表情按钮链路已修复：
  - 点击可正常打开表情面板
  - 选择表情后可插入输入框
  - 面板外点击关闭逻辑已补齐事件目标容错，不再因浏览器事件目标类型差异导致按钮失效
  - 表情浮层已脱离发布卡片裁切容器，不再出现“实际已打开但视觉上像没反应”的假失效
- 快捷发布框已完成一轮真实界面精修：
  - 移除无效“登录可发”提示
  - `@提及` 按钮文案已收敛，不再出现双 `@` 观感
  - 表情按钮改为稳定可见的文本颜文字图标
  - 发布卡片已补一轮二次元轻科幻风格强化

### 3.5 后台邮件投递能力

- 后台常规设置页已补齐 SMTP 邮件服务器配置区，不再只有“管理员邮箱”而没有真实投递配置
- 当前邮件能力已改为优先读取后台设置中的 SMTP 配置：
  - 密码重置邮件
  - 新 IP 登录提醒
  - 评论通知
- 未配置 SMTP 时，密码重置仍保持写入本地调试投递记录，不伪装成真实可发邮件状态

## 4. 当前已收口的后台重点结果

### 4.1 新建文章页 `/rewrz-admin/posts/new`

- 编辑器初始“有未保存更改”误报已修复
- Toast UI 编辑器正文、预览、标签页、弹层已接入后台主题变量
- Markdown 左侧真实输入层 `.toastui-editor-md-container .ProseMirror` 已修复为跟随主题，不再左黑右白
- 自定义 `editor-toolbar` 按钮链路已恢复，不再因脚本前段异常导致整排失效
- 当前已确认有效的自定义工具：
  - 字号缩放
  - 全屏切换
  - 上传文件
  - 插入媒体
  - 评论后可见
  - Markdown / 富文本切换

### 4.2 文章管理页 `/rewrz-admin/posts`

- 顶部操作区、表格、状态标签、分页、空状态已收口到统一后台语义层
- 筛选区已取消特化 grid 布局，改回与其他后台列表页一致的统一过滤条结构：
  - `admin-filter-panel admin-filter-grid flex flex-wrap items-end gap-3 rounded-2xl p-4`
- 本轮应用内浏览器顺序点验中，筛选区、批量操作区、分页区均可正常显示，后台登录态可直接连续使用

### 4.3 页面 / 分类 / 标签 / 用户 / 媒体

- 页面管理、分类管理、标签管理筛选条、统计卡、表格、空状态、分页风格已统一
- 用户管理表格、资料表单、头像/封面区、按钮系统已统一
- 媒体库头部、上传区、筛选区、列表/网格模式切换、详情区已统一进入后台主题体系

### 4.4 常规设置页 `/rewrz-admin/settings`

- 常规设置已补齐 SMTP 配置区：
  - SMTP 服务器
  - 端口
  - 用户名
  - 密码 / 授权码
  - 发件人邮箱
  - STARTTLS / SSL 开关
- “密码重置、邮箱通知事实上不可用”的缺口已收口到当前设计
- 本轮应用内浏览器复测中，上述 SMTP 配置字段均可直接见到，设置页未再出现“只有管理员邮箱、缺少真实投递配置”的旧状态

### 4.5 仪表盘 `/rewrz-admin/dashboard`

- 上次遗留的动态主题残留按钮已继续收口：
  - `重置布局`
  - `创建草稿`
  - `进入评论管理`
  - 评论快捷操作中的 `批准` / `删除`
- 当前这些按钮已接入后台按钮语义层，不再以浅底默认按钮破坏深色主题一致性

## 5. 当前仍需继续巡检的范围

以下不是旧结论，而是截至当前仍应继续覆盖的真实浏览器巡检范围：

- 前台不同内容形态的完整复测：
  - 加密文章
  - 评论可见文章
  - 不同格式文章渲染表现
  - 微博快捷发布深水链路：
    - 真实媒体上传
    - 预览态与编辑态往返切换
    - 实际发布成功后的时间线回流与内容落库
- 后台剩余低频页与边角交互：
  - 继续切主题复测内部局部区域是否还有漏网固定色
  - 继续验证新增/编辑/筛选/分页/批量操作的连续使用体验
  - 继续用真实 SMTP 账号做一次完整发信回归：
    - 找回密码邮件
    - 新 IP 登录提醒
    - 评论通知
- 继续真实浏览器顺序巡检：
  - 不回头重复已收口页面
  - 只记录新问题或最终态变化

## 6. 当前文档维护规则

- 只保留当前有效状态
- 旧的过程记录、临时 blocker、已过时“待复核”结论不再保留
- 后续仅在以下情况下更新：
  - 出现新的真实问题
  - 已有最终结论被后续修复覆盖
  - 巡检范围或当前有效状态发生变化
